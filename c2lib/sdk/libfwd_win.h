#pragma once

struct tagBITMAPINFO; //宣言だけしておく。※定義は windows.h の中にある
typedef tagBITMAPINFO BITMAPINFO;

struct tagRECT; //宣言だけしておく。※定義は windows.h の中にある
typedef tagRECT RECT;

struct tagSIZE; //宣言だけしておく。※定義は windows.h の中にある
typedef tagSIZE SIZE;

struct tagRGBQUAD; //宣言でけしておく。※定義は windows.h の中にある
typedef tagRGBQUAD RGBQUAD;

struct tagPOINT; //宣言だけしておく。※定義は windows.h の中にある
typedef tagPOINT POINT;

typedef unsigned short WORD;
typedef unsigned long  DWORD;

typedef DWORD COLORREF;

typedef void* HANDLE;
